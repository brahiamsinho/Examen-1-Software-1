# FastAPI — ejemplo (desarrollo)
# En Docker Compose se define APP_ENV en el compose; uvicorn escucha en 8000.

APP_ENV=development

# Reservado para integraciones posteriores
# MONGODB_URI=mongodb://localhost:27017/tramitesdb
# OPENAI_API_KEY=
